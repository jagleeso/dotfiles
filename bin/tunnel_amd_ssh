#!/bin/bash
source $(dirname $0)/common.sh

local_port=8686
remote_syslab_port=8686
remote_xen1_port=22
remote_dst_ip="$REMOTE_AMD_IP"

# tunnel_everything \
#     $local_port \
#     $remote_syslab_port \
#     $remote_xen1_port

tunnel_to_intrm \
    $local_port \
    $remote_syslab_port \
    $remote_xen1_port \
    $remote_dst_ip
